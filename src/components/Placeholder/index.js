import React, { Fragment } from "react";
import Container from "../Container";

const Placeholder = () => (
  <Fragment>
    <Container animated />
    <Container animated />
    <Container animated />
  </Fragment>
);

export default Placeholder;
